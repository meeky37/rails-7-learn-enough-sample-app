<% provide(:ttle, "Forgot password") %>
<h1>Forogt password</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
  <%
=begin%>
 there is no instance of password_reset, no password_reset model so @password_reset is wrong 
<%
=end%>
  <%= form_with(url: password_resets_path, scope: :password_reset) do |f| %>
    <%= f.label :email %>
    <%= f.email_field :email, class: 'form-control' %>

    <%= f.submit "Submit", class: "btn btn-primary" %>
   <% end %>
  </div>
</div>